# Actor assets

Drop the `PLACEBO` video file for the `SELECTED ACTING WORK` section into this folder.

Suggested filename:

- `PLACEBO.mp4`

When the file is ready, I can switch the section from the embedded link to the local video while keeping the same layout as the FILM carousel.
